import {FunctionBox_Value_never} from "../FunctionBox_Value_never"
import {type_assert_never} from "../helper/type_assert_never"

export namespace test {
	export const FunctionBox_Value_test=type_assert_never<FunctionBox_Value_never>()
}
