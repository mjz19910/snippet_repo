#include "HTMLToken.pre.ts"

#define VERIFY(expr) \
    (!(expr)   \
        ? ak_verification_failed([#expr,"\n",__FILE__,":",__stringify(__LINE__)].join("")) \
        : void 0)

export class HTMLToken extends HTMLTokenBase {
    opt(): Optional<HTMLTokenBase> {
        return new Optional(this);
    }
}
