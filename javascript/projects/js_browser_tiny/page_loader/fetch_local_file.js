import {FetchRequestState} from "./FetchRequestState.js";

/**
 * @param {string} _filename
 */
export function fetch_local_file(_filename) {
	let fetch_state=new FetchRequestState(null);
	fetch_state;
}